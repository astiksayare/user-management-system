
import * as Mui from "@mui/material";

const theme = Mui.createTheme({
    palette: {
        // mode: 'light',
        primary: {
            main: "#9747ff"
        }
    },
    typography: {
        fontFamily: 'Roboto',
        fontSize: 16
    }
});

export default theme;